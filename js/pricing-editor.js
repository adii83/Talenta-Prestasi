const pricingDefaults={active:true,eyebrow:'Biaya Pendaftaran',title:'',description:'',variant:'single',ornament:true,packages:[pricingPackage('Semua Jenjang','Rp150.000','per peserta, semua jenjang','wallet-cards')],features:[pricingFeature('Sertifikat digital'),pricingFeature('Akses materi'),pricingFeature('ID Card peserta')],action:{...iconItem('Daftar Sekarang','#','arrow-right','primary',false),active:false}};
function pricingPackage(name,price,unit,icon){return{name,price,oldPrice:'',promo:'',unit,note:'',featured:false,active:true,iconMode:'library',libraryIcon:icon,uploadedIcon:'',iconAlt:''}}
function pricingFeature(label){return{label,active:true}}
let pricingState={...structuredClone(pricingDefaults),...(state.pricing||{})};pricingState.packages=(pricingState.packages||pricingDefaults.packages).map(x=>({...pricingDefaults.packages[0],...x}));pricingState.features=pricingState.features||structuredClone(pricingDefaults.features);pricingState.action={...pricingDefaults.action,...(pricingState.action||{})};state.pricing=pricingState;
document.addEventListener('DOMContentLoaded',()=>{bindPricing();syncPricing();renderPricingAll();icons()});
function bindPricing(){bindText(['pricingEyebrow','pricingTitle','pricingDescription'],pricingState,renderPricing);bindToggle('pricingActive',pricingState,renderPricing);document.getElementById('pricingVariant').onchange=e=>{pricingState.variant=e.target.value;renderPricing()};document.getElementById('pricingOrnament').onchange=e=>{pricingState.ornament=e.target.checked;renderPricing()};document.getElementById('addPricingPackage').onclick=()=>{pricingState.packages.push(pricingPackage('Paket baru','Rp0','per peserta','wallet-cards'));renderPricingPackages();renderPricing()};document.getElementById('addPricingFeature').onclick=()=>{pricingState.features.push(pricingFeature('Fasilitas baru'));renderPricingFeatures();renderPricing()};bindPreview('[data-pricing-preview]','pricingPreviewFrame','pricing-preview-frame','pricingPreview');document.getElementById('homeEditorForm').addEventListener('submit',()=>{state.pricing=pricingState;localStorage.setItem(HOME_KEY,JSON.stringify(state))})}
function syncPricing(){Object.entries({pricingActive:pricingState.active,pricingEyebrow:pricingState.eyebrow,pricingTitle:pricingState.title,pricingDescription:pricingState.description,pricingVariant:pricingState.variant,pricingOrnament:pricingState.ornament}).forEach(([id,v])=>{const e=document.getElementById(id);if(e.type==='checkbox')e.checked=v;else e.value=v})}
function renderPricingAll(){renderPricingPackages();renderPricingFeatures();renderPricingAction();renderPricing()}
function renderPricingPackages(){const root=document.getElementById('pricingPackageEditor');root.innerHTML='';pricingState.packages.forEach((pkg,i)=>{const el=document.createElement('article');el.className='pricing-editor-card';el.innerHTML=`<div class="schedule-editor-card__head"><span class="admin-step">${String(i+1).padStart(2,'0')}</span><strong>Paket Harga</strong>${toggleHtml(pkg)}<button type="button" class="repeat-row__delete"><i data-lucide="trash-2"></i></button></div><div class="admin-form-grid"><div class="admin-field"><label>Nama paket / jenjang</label><input class="form-input" data-k="name" value="${esc(pkg.name)}"></div><div class="admin-field"><label>Harga utama</label><input class="form-input" data-k="price" value="${esc(pkg.price)}"></div><div class="admin-field"><label>Harga lama (opsional)</label><input class="form-input" data-k="oldPrice" value="${esc(pkg.oldPrice)}"></div><div class="admin-field"><label>Label promo</label><input class="form-input" data-k="promo" value="${esc(pkg.promo)}"></div><div class="admin-field"><label>Keterangan harga</label><input class="form-input" data-k="unit" value="${esc(pkg.unit)}"></div><div class="admin-field"><label>Catatan paket</label><input class="form-input" data-k="note" value="${esc(pkg.note)}"></div></div><label class="editor-check"><input type="checkbox" data-featured ${pkg.featured?'checked':''}> Tandai sebagai paket unggulan</label>${iconControl(pkg,'pricing-'+i)}`;el.querySelectorAll('[data-k]').forEach(x=>x.oninput=()=>{pkg[x.dataset.k]=x.value;renderPricing()});wireItemToggle(el,pkg,renderPricing);el.querySelector('[data-featured]').onchange=e=>{pkg.featured=e.target.checked;renderPricing()};wireIcon(el,pkg,renderPricing);el.querySelector('.repeat-row__delete').onclick=()=>removeItem(pricingState.packages,i,'paket harga',renderPricingPackages,renderPricing);root.appendChild(el)});icons()}
function renderPricingFeatures(){const root=document.getElementById('pricingFeatureEditor');root.innerHTML='';pricingState.features.forEach((f,i)=>{const el=document.createElement('div');el.className='repeat-row';el.innerHTML=`<span class="repeat-row__grip"><i data-lucide="check-circle"></i></span><input class="form-input" value="${esc(f.label)}">${toggleHtml(f)}<button type="button" class="repeat-row__delete"><i data-lucide="trash-2"></i></button>`;el.querySelector('.form-input').oninput=e=>{f.label=e.target.value;renderPricing()};wireItemToggle(el,f,renderPricing);el.querySelector('button').onclick=()=>removeItem(pricingState.features,i,'fasilitas',renderPricingFeatures,renderPricing);root.appendChild(el)});icons()}
function renderPricingAction(){const a=pricingState.action,root=document.getElementById('pricingActionEditor');root.innerHTML=`<div class="action-editor__card"><div class="action-editor__title"><strong>Tombol Biaya</strong>${toggleHtml(a)}</div><div class="admin-form-grid"><div class="admin-field"><label>Teks tombol</label><input class="form-input" data-k="label" value="${esc(a.label)}"></div><div class="admin-field"><label>URL tujuan</label><input class="form-input" data-k="url" value="${esc(a.url)}"></div><div class="admin-field"><label>Gaya</label><select class="form-input" data-k="style"><option value="primary" ${a.style==='primary'?'selected':''}>Utama</option><option value="outline" ${a.style==='outline'?'selected':''}>Outline</option></select></div></div>${iconControl(a,'pricing-action')}<label class="editor-check"><input type="checkbox" data-newtab ${a.newTab?'checked':''}> Buka di tab baru</label></div>`;root.querySelectorAll('[data-k]').forEach(x=>x.oninput=()=>{a[x.dataset.k]=x.value;renderPricing()});wireItemToggle(root,a,renderPricing);root.querySelector('[data-newtab]').onchange=e=>a.newTab=e.target.checked;wireIcon(root,a,renderPricing);icons()}
function renderPricing(){const root=document.getElementById('pricingPreview'),p=pricingState,t=theme();applyTheme(root,t);if(!p.active)return disabled(root,'Biaya Pendaftaran');const packages=p.packages.filter(x=>x.active),features=p.features.filter(x=>x.active);root.className=`pricing-preview pricing-preview--${p.variant}${p.ornament?' pricing-preview--ornament':''}`;root.innerHTML=`<header><span>${esc(p.eyebrow)}</span>${p.title?`<h2>${esc(p.title)}</h2>`:''}${p.description?`<p>${esc(p.description)}</p>`:''}</header><div class="pricing-preview__packages">${packages.map(pkg=>`<article class="${pkg.featured?'is-featured':''}">${pkg.promo?`<b class="pricing-preview__promo">${esc(pkg.promo)}</b>`:''}<div class="pricing-preview__package-icon">${iconMarkup(pkg)}</div><strong class="pricing-preview__name">${esc(pkg.name)}</strong>${pkg.oldPrice?`<del>${esc(pkg.oldPrice)}</del>`:''}<div class="pricing-preview__amount">${esc(pkg.price)}</div><p class="pricing-preview__unit">${esc(pkg.unit)}</p>${pkg.note?`<p class="pricing-preview__note">${esc(pkg.note)}</p>`:''}<div class="pricing-preview__features">${features.map(f=>`<span><i data-lucide="check"></i>${esc(f.label)}</span>`).join('')}</div>${p.action.active?`<a class="pricing-preview__action ${p.action.style==='outline'?'is-outline':''}">${esc(p.action.label)} ${iconMarkup(p.action)}</a>`:''}</article>`).join('')}</div>`;icons()}
